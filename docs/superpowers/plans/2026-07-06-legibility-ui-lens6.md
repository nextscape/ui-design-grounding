# legibility-ui レンズ⑥追加 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `legibility-ui` スキルに新レンズ「⑥ 現在地の不透明さ（Orientation Loss）」を追加し、5レンズ体制から6レンズ体制へ拡張する。

**Architecture:** `skills/ui-design-grounding/reference/legibility.md`（判断基準・出力フォーマット・マッピング）と `skills/legibility-ui/SKILL.md`（front matter・Phase 1手順）の2ファイルを更新する。既存①〜⑤のセクションは変更せず、⑥を追加するのみ。

**Tech Stack:** Markdown（プレーンテキスト）のみ。ビルド・テスト・リント不要。検証は Read/Grep によるファイル内容の直接確認で行う。

## Global Constraints

- 全スキルコンテンツは日本語で記述する。
- 既存①（見た目と機能の不一致）のセクション本文は変更しない。⑥は「要素の有無・内容」を扱い、①は「要素の視覚的重み」を扱う役割分担を保つ。
- ⑥は⑤（画面間一貫性）と異なり、複数画面の比較を必要とせず、対象画面数によらず常に実施する。
- 対象ブランチ: `feat/legibility-ui-integration`（既存のlegibility-ui統合作業の続き、mainには未マージ）。

---

### Task 1: `reference/legibility.md` にレンズ⑥を追加

**Files:**
- Modify: `skills/ui-design-grounding/reference/legibility.md`

**Interfaces:**
- Consumes: なし
- Produces: レンズ⑥の判断基準。Task 2 の `SKILL.md` から `reference/legibility.md` として参照される。

- [ ] **Step 1: 見出しを更新する**

3行目を変更する。

変更前:
```
## 5つのレンズ
```

変更後:
```
## 6つのレンズ
```

- [ ] **Step 2: ⑤のセクション末尾（76行目、`## 対象外`の直前）に⑥のセクションを追加する**

75行目「**判断が割れやすいケース**: エンティティの性質上、...」の段落の後、76行目の空行の後、77行目「## 対象外（他スキルに委ねる観点）」の直前に、以下を挿入する。

```markdown

### ⑥ 現在地の不透明さ（Orientation Loss）

**問い**: 今、画面のどこ（どの階層・どのセクション）にいるかを、パンくず・見出し・ナビゲーションの状態表示などの手がかりから初見で答えられるか？

**チェック方法**: 画面を開いた瞬間に「これは全体構造のどこに位置する画面か」を自問する。答えられない場合、パンくず・ページ見出し・サイドナビ/タブのアクティブハイライトの**有無**を確認する（視覚的な強弱の判定は①に委ねる）。存在する場合は、その内容が実際のコンテンツ・階層と一致しているかを確認する。

**典型例**:
- 複数階層があるのに、パンくず・現在地表示が画面のどこにも存在しない
- ページ見出しが実際のコンテンツと一致しない、または見出し自体が無い
- サイドバー/タブのナビゲーションで、選択中の項目を示すアクティブ状態の表現（ハイライト・下線等）が一切ない
- 深い階層（3階層以上）なのに、パンくずが直近1階層しか表示しない、または全て省略記号になっている

**判断が割れやすいケース**: 階層が実質1段しかないシンプルな画面構成（トップページのみ等）では、現在地表示自体が不要な場合がある。複数階層が実在するのにその手がかりが一切欠けている場合のみ指摘する。①（見た目と機能の不一致）とは異なり、「表示要素はあるが階層の視覚的重みが弱い」場合はこちらではなく①で扱う。

**実施範囲**: ⑤（画面間一貫性）とは異なり、複数画面の比較は不要。単一画面内で完結する判定のため、対象画面数によらず常に実施する（①〜④と同じ扱い）。
```

- [ ] **Step 3: 出力フォーマットのテンプレートを更新する**

現在の「## 出力フォーマット」セクション内のMarkdownコードブロック（旧98-130行目付近）を、以下の内容に全文置換する。

変更前:
```markdown
## 出力フォーマット

\`\`\`markdown
## legibility-ui レビュー結果

対象: <画面名一覧> / 実施レンズ: ①②③④(⑤)

### ① 見た目と機能の不一致
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ② 目的の不透明さ
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ③ スコープ不整合
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ④ 重複・冗長表示
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ⑤ 画面間一貫性（対象2画面以上のみ）
| 概念 | 画面A | 画面B | 指摘 | 重篤度 |
|---|---|---|---|---|

## 対象外として見送った観点（参考）
- <観点> — <一言> — 委譲先 \`/xxx-ui\`

## 推奨アクション（優先度順）
1. \`/xxx-ui\` — 対応件数（代表例）
\`\`\`
```

変更後:
```markdown
## 出力フォーマット

\`\`\`markdown
## legibility-ui レビュー結果

対象: <画面名一覧> / 実施レンズ: ①②③④⑥(⑤)

### ① 見た目と機能の不一致
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ② 目的の不透明さ
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ③ スコープ不整合
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ④ 重複・冗長表示
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ⑥ 現在地の不透明さ
| 画面 | 指摘 | 重篤度 | 該当コード |
|---|---|---|---|

### ⑤ 画面間一貫性（対象2画面以上のみ）
| 概念 | 画面A | 画面B | 指摘 | 重篤度 |
|---|---|---|---|---|

## 対象外として見送った観点（参考）
- <観点> — <一言> — 委譲先 \`/xxx-ui\`

## 推奨アクション（優先度順）
1. \`/xxx-ui\` — 対応件数（代表例）
\`\`\`
```

- [ ] **Step 4: 推奨アクションのマッピング表に⑥の行を追加する**

変更前:
```
| レンズ | 主なマッピング先 |
|---|---|
| ①見た目と機能の不一致 | `/clarify-ui`（用語・視覚的手がかりの改善）、`/arrange-ui`（視覚的グルーピング・階層の修正） |
| ②目的の不透明さ | `/clarify-ui`（説明追加・表示タイミング調整）、`/design-ui`（機能自体の要否再設計） |
| ③スコープ不整合 | `/design-ui`（情報設計の見直し）、`/arrange-ui`（配置変更） |
| ④重複・冗長表示 | `/slim-ui`（重複要素の削除）、`/clarify-ui`（使い分けの明確化） |
| ⑤画面間一貫性 | `/extract-ui`（パターン統一・コンポーネント化）、`/design-ui` |
```

変更後:
```
| レンズ | 主なマッピング先 |
|---|---|
| ①見た目と機能の不一致 | `/clarify-ui`（用語・視覚的手がかりの改善）、`/arrange-ui`（視覚的グルーピング・階層の修正） |
| ②目的の不透明さ | `/clarify-ui`（説明追加・表示タイミング調整）、`/design-ui`（機能自体の要否再設計） |
| ③スコープ不整合 | `/design-ui`（情報設計の見直し）、`/arrange-ui`（配置変更） |
| ④重複・冗長表示 | `/slim-ui`（重複要素の削除）、`/clarify-ui`（使い分けの明確化） |
| ⑥現在地の不透明さ | `/design-ui`（ナビゲーション・情報設計の見直し）、`/arrange-ui`（パンくず・見出しの追加・視覚的強化） |
| ⑤画面間一貫性 | `/extract-ui`（パターン統一・コンポーネント化）、`/design-ui` |
```

- [ ] **Step 5: 検証する**

```bash
grep -n "## 6つのレンズ\|^### ⑥ 現在地の不透明さ" "skills/ui-design-grounding/reference/legibility.md"
grep -c "^### ⑥" "skills/ui-design-grounding/reference/legibility.md"
grep -n "①②③④⑥(⑤)" "skills/ui-design-grounding/reference/legibility.md"
grep -n "⑥現在地の不透明さ" "skills/ui-design-grounding/reference/legibility.md"
```

Expected:
- 1行目のコマンドで3件（`## 6つのレンズ` 1件、`### ⑥ 現在地の不透明さ` で始まる行が本文セクション見出しと出力フォーマット例内の見出しの計2件、合計3件）
- 2行目のコマンドで `2`（本文セクション見出し1件＋出力フォーマット例内の見出し1件）
- 3行目のコマンドで1件（出力フォーマットの実施レンズ表記）
- 4行目のコマンドで1件（マッピング表の行）

さらに、既存①のセクション本文が変更されていないことを確認する:
```bash
grep -n "この観点は対象画面が1画面のときも Phase 1 で必ずチェックする" "skills/ui-design-grounding/reference/legibility.md"
```
Expected: ①のセクション内（18行目付近）に1件出力される（①の記述が保持されている証跡）。

- [ ] **Step 6: Commit**

```bash
git add skills/ui-design-grounding/reference/legibility.md
git commit -m "$(cat <<'EOF'
feat(legibility-ui): レンズ⑥「現在地の不透明さ」を追加

既存5レンズがカバーしない「画面全体における現在地の把握可能性」を
新レンズとして追加する。既存①(見た目と機能の不一致)は現在地表示の
視覚的重みを、⑥は現在地表示の要素の有無・内容を扱う役割分担とし、
重複を避けた。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `legibility-ui/SKILL.md` にレンズ⑥のチェック手順を追加

**Files:**
- Modify: `skills/legibility-ui/SKILL.md`

**Interfaces:**
- Consumes: `skills/ui-design-grounding/reference/legibility.md` のレンズ⑥定義（Task 1 で作成済み）
- Produces: なし

- [ ] **Step 1: front matter の description を更新する**

変更前:
```
description: 実装を読む前に、画面を初見で見たときの分かりやすさを5つの観点（見た目と機能の一致・機能の目的の透明性・コントロールのスコープ整合性・重複と冗長表示・画面間の設計一貫性）で監査する。UIが「使えば分かる」を前提にしていないか、初見のユーザー視点で確認したいときに使う。トリガ例「初見で分かりづらい箇所をレビューして」「UIUXレビュー」「この画面何をするか分からない」「画面同士の一貫性をチェック」「同じ情報が重複していないか確認して」。
```

変更後:
```
description: 実装を読む前に、画面を初見で見たときの分かりやすさを6つの観点（見た目と機能の一致・機能の目的の透明性・コントロールのスコープ整合性・重複と冗長表示・画面間の設計一貫性・現在地の把握しやすさ）で監査する。UIが「使えば分かる」を前提にしていないか、初見のユーザー視点で確認したいときに使う。トリガ例「初見で分かりづらい箇所をレビューして」「UIUXレビュー」「この画面何をするか分からない」「画面同士の一貫性をチェック」「同じ情報が重複していないか確認して」「今どこにいるか分からない」「パンくずが無くて迷子になる」。
```

- [ ] **Step 2: Phase 1 の手順に手順7を追加する**

Phase 1 セクション内、既存の手順6（「画面内に同時に見えている同種コンポーネント...」で始まる行）の直後に、新しい手順7を追加する。

変更前（末尾部分）:
```
6. 画面内に同時に見えている同種コンポーネント（入力欄・ドロップダウン・ボタン等）どうしのサイズ・高さ・余白を比べ、不揃いがないか確認する。これは1画面のレビューでも毎回行う（レンズ①、`reference/legibility.md` 参照）。スクリーンショットで確認する。

## Phase 2 — 横断一貫性監査（対象2画面以上のときのみ）
```

変更後:
```
6. 画面内に同時に見えている同種コンポーネント（入力欄・ドロップダウン・ボタン等）どうしのサイズ・高さ・余白を比べ、不揃いがないか確認する。これは1画面のレビューでも毎回行う（レンズ①、`reference/legibility.md` 参照）。スクリーンショットで確認する。
7. 画面を開いた瞬間に「これは全体構造のどこに位置する画面か」を自問し、パンくず・ページ見出し・ナビゲーションのアクティブ状態などの現在地手がかりが存在するか、存在する場合は内容が実態と一致しているかを確認する。これは1画面のレビューでも毎回行う（レンズ⑥、`reference/legibility.md` 参照）。

## Phase 2 — 横断一貫性監査（対象2画面以上のときのみ）
```

- [ ] **Step 3: 検証する**

```bash
grep -n "6つの観点" "skills/legibility-ui/SKILL.md"
grep -n "現在地の把握しやすさ" "skills/legibility-ui/SKILL.md"
grep -n "今どこにいるか分からない" "skills/legibility-ui/SKILL.md"
grep -n "^7\. 画面を開いた瞬間に" "skills/legibility-ui/SKILL.md"
```

Expected: 4つのコマンドすべてが1件ずつ出力される。

- [ ] **Step 4: Commit**

```bash
git add skills/legibility-ui/SKILL.md
git commit -m "$(cat <<'EOF'
feat(legibility-ui): SKILL.mdにレンズ⑥のチェック手順を追加

Phase 1 に現在地把握のチェック手順(手順7)を追加し、front matterの
説明・トリガ例を6レンズ体制に合わせて更新する。

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 整合性の最終検証

**Files:**
- なし（検証のみ、変更は加えない）

**Interfaces:**
- Consumes: Task 1〜2 の全成果物
- Produces: なし

- [ ] **Step 1: レンズ番号の一貫性を確認する**

```bash
grep -n "^### ①\|^### ②\|^### ③\|^### ④\|^### ⑤\|^### ⑥" "skills/ui-design-grounding/reference/legibility.md"
```

Expected: 本文セクション見出し6件（①②③④⑤⑥、この順）＋出力フォーマット例内の見出し6件（①②③④⑥⑤、この順）、計12件が出力される。本文セクションの並び順が①②③④⑤⑥（レンズ定義順）、出力フォーマット例の並び順が①②③④⑥⑤（実施頻度順、⑤のみ条件付き）であることを目視確認する。

- [ ] **Step 2: 既存①〜⑤の本文が変更されていないことを確認する**

```bash
grep -c "^###" "skills/ui-design-grounding/reference/legibility.md"
grep -n "^## " "skills/ui-design-grounding/reference/legibility.md"
```

Expected: 1つ目のコマンドで `12`（本文セクション見出し①〜⑥=6件＋出力フォーマットのコードブロック内の見出し①②③④⑥⑤=6件。Task 1適用前は `10` である）。

2つ目のコマンド（`##` レベルの見出し。出力フォーマットのコードブロック内の見出しも `^## ` に一致するため、Markdownの構造ではなくテキスト上の行として8件ヒットする点に注意）は以下の8行が出力される:
```
## 6つのレンズ
## legibility-ui レビュー結果
## 対象外（他スキルに委ねる観点）
## 対象外として見送った観点（参考）
## 推奨アクション（優先度順）
## 重篤度
## 出力フォーマット
## 推奨アクションのマッピング
```
（Task 1適用前は1行目が `## 5つのレンズ` である点のみ異なり、他7行・出現順序は同じ。）

- [ ] **Step 3: SKILL.md側の6レンズ表記とreference側の定義が矛盾しないことを確認する**

```bash
grep -n "reference/legibility.md" "skills/legibility-ui/SKILL.md"
```

Expected: 8行が出力される（Task 2適用前は7行。内訳: MANDATORY PREPARATION 1行、Phase 1 手順2「①②」1行、手順4「③」1行、手順5「④」1行、手順6「①」1行、手順7「⑥」1行〈新規〉、Phase 2「⑤」1行、Phase 4 1行〈同一行内に2回出現するが `grep -n` は行単位のため1行としてカウント〉）。パスの綴りに誤りがないことを確認する。

- [ ] **Step 4: git status で作業ツリーがクリーンであることを確認する**

```bash
git status --short
git log --oneline -4
```

Expected: 作業ツリーはクリーン。直近4コミットに Task 1・Task 2 の2コミットが含まれている。
