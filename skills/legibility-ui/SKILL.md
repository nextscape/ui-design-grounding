---
name: legibility-ui
description: 実装を読む前に、画面を初見で見たときの分かりやすさを7つの観点（見た目と機能の一致・現在地の把握しやすさ・機能の目的の透明性・コントロールのスコープ整合性・重複と冗長表示・操作群の構造・画面間の設計一貫性）で監査する。UIが「使えば分かる」を前提にしていないか、初見のユーザー視点で確認したいときに使う。トリガ例「初見で分かりづらい箇所をレビューして」「UIUXレビュー」「この画面何をするか分からない」「画面同士の一貫性をチェック」「同じ情報が重複していないか確認して」「今どこにいるか分からない」「パンくずが無くて迷子になる」「ボタンが並びすぎて何から押せばいいか分からない」。
user-invocable: true
argument-hint: "[対象画面 (画面名／機能横断／省略で全画面)]"
---

# legibility-ui — 初見の分かりやすさ監査

score-ui が「実装を読んでからニールセンの10ヒューリスティクスで評価する」のに対し、本スキルは**実装を一切読まずに、画面だけを見た初見の人間としてどう見えるか**を出発点にする。先にコードを読むと実装意図に引きずられ、初見での誤解を検出できなくなるため、**観察の順序を厳守する**（画面を見る→判断する→最後にコードを裏付けとして見る）。

## 準備（MANDATORY PREPARATION）

ui-design-grounding スキルを呼び出し、以下のリファレンスを読み込む:

- `ui-design-grounding/reference/legibility.md`
- `ui-design-grounding/reference/playwright.md`
- `ui-design-grounding/reference/ui-report.md`
- `ui-design-grounding/reference/design-md-gate.md`

## Phase 0 — 対象確定・基準確認・準備

### 対象確定

呼び出し引数を解釈する。

- 画面名の指定（例:「Home/Tasks/Notesをチェック」）→ 指定画面のみを対象にする。
- 「機能横断」「一連の流れ」等の指定 → 関連する画面をまとめて対象化する（例: タスク登録・編集・一覧）。
- 指定なし／「全画面」 → 一般的なルーター定義パターン（React Router のルート定義ファイル、Next.js の `app/` ディレクトリ構造、Vue Router のルーター設定ファイル 等）を Glob/Grep で探索し、画面一覧を推定する（この探索は対象範囲の把握のみが目的で、Phase 1 の判断材料にしてはならない）。**見つからなければ、ユーザーに主要画面を確認する。**
- 対象が2画面以上ならレンズ⑦（画面間一貫性）も実施する。1画面のみならレンズ①〜⑥を実施する（⑦のみ2画面以上が必要）。

### 基準の確認（DESIGN.md 前段ゲート）

`design-md-gate.md` の前段ゲートを実施する。DESIGN.md があれば front matter のトークンと本文の指針を基準として読み込み、レンズ⑦（画面間一貫性）で「意図的な差異の正当性」を判定する材料にする。無ければ未整備として扱い `/init-design` を提案する。評価専用のため後段ゲートは不要。

### 準備

1. `playwright.md` の**準備（共通）**を実施する（MCP 利用可否の確認 → アプリ起動 → スクリーンショット保存先の確定）。MCP が使えなければその旨を出力して終了する。
2. **この時点でも対象コンポーネントのソースは読まない。**（本スキル固有の規律。`playwright.md` の「評価系＝観察先行」を最も厳格に適用する。）

## Phase 1 — 素朴初見監査（画面ごと・コードを読まずに実施）

対象画面ごとに以下を行う。**この Phase の間はコンポーネントのソースを一切開かない。**

Playwright 操作は `playwright.md` の**ツール選択マトリクス**に従い、判定の性質で使い分ける。本スキルでのレンズ別の当てはめ:
- **視覚判定**（レンズ①②⑤⑥ — 見た目の重み・現在地手がかりの有無・重複表示・操作群のグルーピングと並び順）→ `browser_take_screenshot`。
- **機能判定**（レンズ③④ — 目的理解・スコープ確認）→ `browser_snapshot`（アクセシビリティツリー）で要素の存在・状態遷移を確認すれば足りる。

1. Playwright で画面を開く。視覚判定が必要なら `browser_take_screenshot`、機能判定のみなら `browser_snapshot` を取得する。
2. 画面内の主要な操作可能要素（ボタン・リンク・フィルタ・入力欄・見出し・バッジ等）を一つずつ挙げ、**先に**「初見でこれは何をするものだと思うか」を言語化する。**要素数が多くてもこの列挙は省略・間引きしない**（要素が多い画面ほど検出漏れが起きやすい）。`reference/legibility.md` のレンズ①③の問いを当てる。
3. 見出しや区切りなしに操作コントロールが4個以上連続して並ぶ領域があれば、ラベルの字面によらず、各操作を「動詞／対象／スコープ／性質」に分解した表を必ず作り、構造の平坦化・作業手順の可読性を判定する（レンズ⑥、`reference/legibility.md` 参照）。
4. 実際にクリック・操作して本当の挙動を確認し、手順2の予想とのギャップを記録する。ギャップがあれば①または③の指摘候補にする。操作結果の確認は `browser_snapshot` で行う。
5. 影響が画面外に及びそうなコントロール（設定変更・名称変更・全体に効くフィルタ等）は、実際に操作して影響範囲を確認し、設置場所の妥当性を判定する。逆に、設置場所から作用対象を推測できないコントロール（対象から離れた共通領域に集約された操作等）もここで拾う（レンズ④、`reference/legibility.md` 参照）。
6. 画面内の見出し・識別子（プロジェクト名・画面名等）と、似た名前・似た役割を持つ複数の項目（フィールド・設定・ボタン）を洗い出し、同じ情報や概念が重複していないか、使い分けが初見で説明できるかを確認する（レンズ⑤、`reference/legibility.md` 参照）。
7. 画面内に同時に見えている同種コンポーネント（入力欄・ドロップダウン・ボタン等）どうしのサイズ・高さ・余白を比べ、不揃いがないか確認する。これは1画面のレビューでも毎回行う（レンズ①、`reference/legibility.md` 参照）。スクリーンショットで確認する。
8. 画面を開いた瞬間に「これは全体構造のどこに位置する画面か」を自問し、パンくず・ページ見出し・ナビゲーションのアクティブ状態などの現在地手がかりが存在するか、存在する場合は内容が実態と一致しているかを確認する。これは1画面のレビューでも毎回行う（レンズ②、`reference/legibility.md` 参照）。

## Phase 2 — 横断一貫性監査（対象2画面以上のときのみ）

Phase 1 で集めたスクリーンショット・操作結果をもとに、画面をまたいで同種の概念（一覧→作成/編集、フィルタUI、ヘッダー/共通chrome など）および同種の基本コンポーネント（主要CTAボタン・ドロップダウン・入力欄のサイズ等）を比較する。実装パターンやビジュアルが画面ごとに異なる箇所を挙げる（レンズ⑦、`reference/legibility.md` 参照）。DESIGN.md が読み込まれていれば、差異が意図的なもの（既存の設計判断に根拠がある）かをそこに照らして判定する。

## Phase 3 — コード特定

Phase 1/2 で見つかった指摘についてのみ、該当コンポーネントを Grep して `file:line` を特定する。**ここで初めてコードを読む。** 判定そのものは Phase 1/2 の「初見の目」を正とし、コードを読んだことを理由に指摘を取り下げない（コードの意図が分かっても、初見で伝わらないなら指摘は有効）。

## Phase 4 — 出力・レポート保存

以下の出力フォーマットに従い、冒頭サマリー、7レンズ一覧、レンズ別の詳細、推奨アクションをまとめる。`reference/legibility.md`「対象外（他スキルに委ねる観点）」に該当する気づき（エルゴノミクス・ピクセル整合・将来のデータ量への堅牢性等）があれば、指摘表には含めず「対象外として見送った観点」として委譲先スキルとともに一言添える。

`ui-report.md` に従い、評価対象プロジェクトの `.design/reports/YYYY-MM-DD/HHmmss-legibility-ui.md` に詳細レポートを保存する。スクリーンショットを取得した場合は `.design/reports/YYYY-MM-DD/screenshots/` に保存し、レポート本文から相対リンクする。会話内の最終応答では、要約・最優先アクション・保存先を短く示す。

保存後、`ui-report.md` の「指摘のフォローアップ（タスク管理への接続）」を必ず実施する — この実行内で修正しない P0/P1 指摘は、プロジェクトのタスク管理へ登録するか、最終応答で登録を提案する（レポートを出して終わりにしない）。

## 出力フォーマット

```markdown
# legibility-ui レポート: <対象>

| 項目 | 内容 |
|---|---|
| スキル | `legibility-ui` |
| 対象 | <画面名一覧> |
| 実施日時 | <ISO 8601形式のローカル日時> |
| DESIGN.md | あり / なし / 未確認 |
| 観察方法 | Playwright MCP / 未実施（理由） |
| レポート保存先 | `.design/reports/YYYY-MM-DD/HHmmss-legibility-ui.md` |

## スクリーンショット

| # | 内容 | パス |
|---|---|---|
| 1 | <画面・状態・幅など> | [screenshots/HHmmss-legibility-ui-01.png](screenshots/HHmmss-legibility-ui-01.png) |

<!-- 取得していない場合は「なし」と書く。 -->

## 初見理解サマリー

| 項目 | 内容 |
|---|---|
| 初見理解の総合判定 | Critical / Needs Improvement / Acceptable / Good |
| P0/P1件数 | P0: X件 / P1: X件 |
| 最も誤解されやすい画面・操作 | <画面名・操作名> |
| 次にやること | <最優先で実行すべき対応を1文で書く> |

## 7レンズ結果

| レンズ | 実施 | 問題数 | 最大重篤度 | 代表的な誤解 |
|---|---|---:|---|---|
| ① 見た目と機能の不一致 | 実施 | X | P0-P3 | ... |
| ② 現在地の不透明さ | 実施 | X | P0-P3 | ... |
| ③ 目的の不透明さ | 実施 | X | P0-P3 | ... |
| ④ スコープ不整合 | 実施 | X | P0-P3 | ... |
| ⑤ 重複・冗長表示 | 実施 | X | P0-P3 | ... |
| ⑥ 操作群の平坦化 | 実施 | X | P0-P3 | ... |
| ⑦ 画面間一貫性 | 実施 / 未実施 | X | P0-P3 / - | <未実施なら理由を書く> |

## レンズ別詳細

### ① 見た目と機能の不一致

| 画面 | 対象 | 初見の予想 | 実際の挙動 | ギャップ | 影響 | 重篤度 | 該当コード | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|
| ... | ... | ... | ... | ... | ... | P0-P3 | `path/to/file:line` / 未特定 | [screenshots/HHmmss-legibility-ui-01.png](screenshots/HHmmss-legibility-ui-01.png) / なし | ... |

### ② 現在地の不透明さ

| 画面 | 対象 | 初見の予想 | 実際の挙動 | ギャップ | 影響 | 重篤度 | 該当コード | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|

### ③ 目的の不透明さ

| 画面 | 対象 | 初見の予想 | 実際の挙動 | ギャップ | 影響 | 重篤度 | 該当コード | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|

### ④ スコープ不整合

| 画面 | 対象 | 初見の予想 | 実際の挙動 | ギャップ | 影響 | 重篤度 | 該当コード | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|

### ⑤ 重複・冗長表示

| 画面 | 対象 | 初見の予想 | 実際の挙動 | ギャップ | 影響 | 重篤度 | 該当コード | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|

### ⑥ 操作群の平坦化

操作コントロールが4個以上連続して並ぶ領域があった場合は、作成した分解表（動詞／対象／スコープ／性質）もここに載せる。

| 画面 | 対象 | 初見の予想 | 実際の挙動 | ギャップ | 影響 | 重篤度 | 該当コード | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|

### ⑦ 画面間一貫性

対象が2画面以上の場合のみ実施する。1画面のみの場合は、次のように未実施理由を明記する。

未実施理由: <対象が1画面のみのため / 比較対象画面を特定できなかったため>

| 概念 | 画面A | 画面B | 初見の予想 | 実際の差異 | ギャップ | 影響 | 重篤度 | 関連スクリーンショット | 推奨対応 |
|---|---|---|---|---|---|---|---|---|---|

## 対象外として見送った観点（参考）

| 観点 | 気づき | 委譲先 |
|---|---|---|
| <観点> | <一言> | `/xxx-ui` |

## 推奨アクション（優先度順）

1. `/[スキル名]` — [対応する問題N件]（[代表的な問題の要約]）
2. `/[スキル名]` — [対応する問題N件]（[代表的な問題の要約]）
3. ...

> 上から順に実行することを推奨。「N番やって」で該当スキルを実行できます。

## 未検証・制約

- <未検証項目または制約。なければ「なし」>
```
